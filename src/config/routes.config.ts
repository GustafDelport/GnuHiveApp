export const routesConfig = {
    home: {
        root: {
            route: '/home',
            endpoint: 'home'
        }
    },
    login: {
        root: {
            route: '/login',
            endpoint: 'login'
        }
    }
};
